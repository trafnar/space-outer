import { NotFoundContent } from "@/components/NotFoundContent";

export default function TestNotFound() {
  return (
    <NotFoundContent
      title="Test not found"
      description="Sorry, that test couldn’t be found."
    />
  );
}
